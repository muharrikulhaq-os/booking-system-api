version: "2"
sql:
  - engine: "postgresql"
    queries: "sql/query"
    schema: "sql/schema"
    gen:
      go:
        package: "repository"
        out: "internal/repository"
        emit_interface: true
        emit_json_tags: true
        emit_pointers_for_null_types: true
